<div class="comments-header">
  <h4>
  	Comments count: <%= commentable.comment_threads.count == 0 ? "no" : commentable.comment_threads.count %> 
  </h4>
</div>

<%= render partial: "comments/form", locals: {new_comment: new_comment} unless new_comment.blank? %>

<div class="comments-container">
  <%= render partial: "comments/reply", locals: {comments: commentable.root_comments} %>
</div>
