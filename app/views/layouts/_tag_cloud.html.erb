<!--app/views/layouts/_tag_cloug.html.erb-->
<div class="tag_cloud row-fluid">
	<% tag_cloud Project.tag_counts, %w(css1 css2 css3 css4) do |tag, css_class| %>
  	<%= link_to tag.name, tag_path(tag.name), :class => css_class %>
	<% end %>
</div>

